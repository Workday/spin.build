entity Reservation {
  // Confirmation UUID
  confirmation_number: String;

  // Day precision
  date_of_issue: Date;

  tickets :: this => [Ticket]            <= Traveler;          // Link to tickets
  routes  :: this => [Route] origin      <= departures Airport,
                             destination <= arrival    Airport;
  type: variant { A, B, C };
}
