<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>build.spin</groupId>
    <artifactId>project</artifactId>
    <version>${revision}</version>

    <packaging>pom</packaging>

    <name>spin.build Project</name>

    <organization>
        <name>Workday, Inc.</name>
        <url>https://www.workday.com</url>
    </organization>

    <inceptionYear>2026</inceptionYear>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>Brian Oliver</name>
            <email>brian.oliver@workday.com</email>
            <roles>
                <role>architect</role>
                <role>developer</role>
            </roles>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/workday/spin.build.git</connection>
        <developerConnection>scm:git:https://github.com/workday/spin.build.git</developerConnection>
        <url>https://github.com/workday/spin.build</url>
    </scm>

    <properties>
        <!-- the version number for all spin modules -->
        <revision>0.1.0-SNAPSHOT</revision>

        <!-- Project Build Properties -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Platform Versions -->
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>

        <!-- Module Dependency Versions -->
        <asm.version>9.9.1</asm.version>
        <base.version>0.21.0-SNAPSHOT</base.version>
        <codemodel.version>0.19.0-SNAPSHOT</codemodel.version>
        <checkstyle.version>13.3.0</checkstyle.version>
        <freemarker.version>2.3.34</freemarker.version>
        <graphql.version>25.0</graphql.version>
        <graphql.servlet.version>16.0.0</graphql.servlet.version>
        <hamcrest.version>3.0</hamcrest.version>
        <jackson.version>2.21.1</jackson.version>
        <jakarta.el.impl.version>4.0.2</jakarta.el.impl.version>
        <jakarta.inject.version>2.0.1</jakarta.inject.version>
        <junit.platform.version>6.0.3</junit.platform.version>
        <junit.version>6.0.3</junit.version>
        <maven.version>3.9.12</maven.version>
        <maven.resolver.version>1.9.27</maven.resolver.version>
        <mockito.version>5.18.1</mockito.version>
        <slf4j.version>2.0.17</slf4j.version>
        <spawn.version>0.1.0-SNAPSHOT</spawn.version>
        <undertow.version>2.3.23.Final</undertow.version>

        <!-- Plugin Dependency Versions -->
        <maven-assembly-plugin.version>3.8.0</maven-assembly-plugin.version>
        <maven-build-helper-plugin.version>3.6.1</maven-build-helper-plugin.version>
        <maven-central-publishing-plugin.version>0.10.0</maven-central-publishing-plugin.version>
        <maven-compiler-plugin.version>3.15.0</maven-compiler-plugin.version>
        <maven-checkstyle-plugin.version>3.6.0</maven-checkstyle-plugin.version>
        <maven-dependency-plugin.version>3.10.0</maven-dependency-plugin.version>
        <maven-deploy-plugin.version>3.1.4</maven-deploy-plugin.version>
        <maven-enforcer-plugin.version>3.6.2</maven-enforcer-plugin.version>
        <maven-exec-plugin.version>3.6.3</maven-exec-plugin.version>
        <maven-flatten-plugin.version>1.7.3</maven-flatten-plugin.version>
        <maven-gpg-plugin.version>3.2.8</maven-gpg-plugin.version>
        <maven-jar-plugin.version>3.5.0</maven-jar-plugin.version>
        <maven-javadoc-plugin.version>3.12.0</maven-javadoc-plugin.version>
        <maven-license-plugin.version>2.7.1</maven-license-plugin.version>
        <maven-resources-plugin.version>3.4.0</maven-resources-plugin.version>
        <maven-source-plugin.version>3.4.0</maven-source-plugin.version>
        <maven-surefire-plugin.version>3.5.5</maven-surefire-plugin.version>
        <maven-versions-plugin.version>2.21.0</maven-versions-plugin.version>
    </properties>

    <modules>
        <module>spin</module>
        <module>spin-api</module>
        <module>spin-common</module>
        <module>spin-engine</module>
        <module>spin-modules</module>
        <module>spin-testing</module>
        <module>spin-collider</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>build.base</groupId>
                <artifactId>base-archiving</artifactId>
                <version>${base.version}</version>
            </dependency>

            <dependency>
                <groupId>build.base</groupId>
                <artifactId>base-flow</artifactId>
                <version>${base.version}</version>
            </dependency>

            <dependency>
                <groupId>build.spawn</groupId>
                <artifactId>spawn-application</artifactId>
                <version>${spawn.version}</version>
            </dependency>

            <dependency>
                <groupId>build.spawn</groupId>
                <artifactId>spawn-local-platform</artifactId>
                <version>${spawn.version}</version>
            </dependency>

            <dependency>
                <groupId>build.spawn</groupId>
                <artifactId>spawn-jdk</artifactId>
                <version>${spawn.version}</version>
            </dependency>

            <dependency>
                <groupId>build.spawn</groupId>
                <artifactId>spawn-local-jdk</artifactId>
                <version>${spawn.version}</version>
            </dependency>

            <dependency>
                <groupId>build.base</groupId>
                <artifactId>base-assertion</artifactId>
                <version>${base.version}</version>
            </dependency>

            <dependency>
                <groupId>build.base</groupId>
                <artifactId>base-network</artifactId>
                <version>${base.version}</version>
            </dependency>

            <dependency>
                <groupId>build.base</groupId>
                <artifactId>base-foundation</artifactId>
                <version>${base.version}</version>
            </dependency>

            <dependency>
                <groupId>build.base</groupId>
                <artifactId>base-io</artifactId>
                <version>${base.version}</version>
            </dependency>

            <dependency>
                <groupId>build.base</groupId>
                <artifactId>base-parsing</artifactId>
                <version>${base.version}</version>
            </dependency>

            <dependency>
                <groupId>build.base</groupId>
                <artifactId>base-table</artifactId>
                <version>${base.version}</version>
            </dependency>

            <dependency>
                <groupId>build.base</groupId>
                <artifactId>base-configuration</artifactId>
                <version>${base.version}</version>
            </dependency>

            <dependency>
                <groupId>build.base</groupId>
                <artifactId>base-option</artifactId>
                <version>${base.version}</version>
            </dependency>

            <dependency>
                <groupId>build.base</groupId>
                <artifactId>base-commandline</artifactId>
                <version>${base.version}</version>
            </dependency>

            <dependency>
                <groupId>build.base</groupId>
                <artifactId>base-expression</artifactId>
                <version>${base.version}</version>
            </dependency>

            <dependency>
                <groupId>build.base</groupId>
                <artifactId>base-telemetry</artifactId>
                <version>${base.version}</version>
            </dependency>

            <dependency>
                <groupId>org.ow2.asm</groupId>
                <artifactId>asm</artifactId>
                <version>${asm.version}</version>
            </dependency>

            <dependency>
                <groupId>org.ow2.asm</groupId>
                <artifactId>asm-tree</artifactId>
                <version>${asm.version}</version>
            </dependency>

            <dependency>
                <groupId>org.ow2.asm</groupId>
                <artifactId>asm-util</artifactId>
                <version>${asm.version}</version>
            </dependency>

            <dependency>
                <groupId>com.graphql-java</groupId>
                <artifactId>graphql-java</artifactId>
                <version>${graphql.version}</version>
            </dependency>

            <dependency>
                <groupId>com.graphql-java-kickstart</groupId>
                <artifactId>graphql-java-servlet</artifactId>
                <version>${graphql.servlet.version}</version>
            </dependency>

            <dependency>
                <groupId>org.hamcrest</groupId>
                <artifactId>hamcrest</artifactId>
                <version>${hamcrest.version}</version>
            </dependency>

            <dependency>
                <groupId>org.glassfish</groupId>
                <artifactId>jakarta.el</artifactId>
                <version>${jakarta.el.impl.version}</version>
            </dependency>

            <dependency>
                <groupId>jakarta.inject</groupId>
                <artifactId>jakarta.inject-api</artifactId>
                <version>${jakarta.inject.version}</version>
            </dependency>

            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>

            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-api</artifactId>
                <version>${junit.version}</version>
            </dependency>

            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-engine</artifactId>
                <version>${junit.version}</version>
            </dependency>

            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-params</artifactId>
                <version>${junit.version}</version>
            </dependency>

            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <version>${mockito.version}</version>
            </dependency>

            <dependency>
                <groupId>build.codemodel</groupId>
                <artifactId>dependency-injection</artifactId>
                <version>${codemodel.version}</version>
            </dependency>

            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>

            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-simple</artifactId>
                <version>${slf4j.version}</version>
            </dependency>

            <dependency>
                <groupId>io.undertow</groupId>
                <artifactId>undertow-core</artifactId>
                <version>${undertow.version}</version>
            </dependency>

            <dependency>
                <groupId>io.undertow</groupId>
                <artifactId>undertow-servlet</artifactId>
                <version>${undertow.version}</version>
            </dependency>

            <dependency>
                <groupId>com.fasterxml.jackson.datatype</groupId>
                <artifactId>jackson-datatype-jdk8</artifactId>
                <version>${jackson.version}</version>
            </dependency>

            <dependency>
                <groupId>org.freemarker</groupId>
                <artifactId>freemarker</artifactId>
                <version>${freemarker.version}</version>
            </dependency>

            <!-- Maven Dependencies -->
            <dependency>
                <groupId>org.apache.maven</groupId>
                <artifactId>maven-core</artifactId>
                <version>${maven.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.maven</groupId>
                <artifactId>maven-settings-builder</artifactId>
                <version>${maven.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.maven</groupId>
                <artifactId>maven-resolver-provider</artifactId>
                <version>${maven.version}</version>
            </dependency>

            <!-- Maven Resolver Dependencies -->
            <dependency>
                <groupId>org.apache.maven.resolver</groupId>
                <artifactId>maven-resolver-api</artifactId>
                <version>${maven.resolver.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.maven.resolver</groupId>
                <artifactId>maven-resolver-spi</artifactId>
                <version>${maven.resolver.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.maven.resolver</groupId>
                <artifactId>maven-resolver-connector-basic</artifactId>
                <version>${maven.resolver.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.maven.resolver</groupId>
                <artifactId>maven-resolver-impl</artifactId>
                <version>${maven.resolver.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.maven.resolver</groupId>
                <artifactId>maven-resolver-transport-file</artifactId>
                <version>${maven.resolver.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.maven.resolver</groupId>
                <artifactId>maven-resolver-transport-http</artifactId>
                <version>${maven.resolver.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.maven.resolver</groupId>
                <artifactId>maven-resolver-util</artifactId>
                <version>${maven.resolver.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <reporting>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <configuration>
                    <configLocation>config/checkstyle/checkstyle.xml</configLocation>
                    <consoleOutput>true</consoleOutput>
                    <failsOnError>true</failsOnError>
                </configuration>

                <reportSets>
                    <reportSet>
                        <reports>
                            <report>checkstyle</report>
                        </reports>
                    </reportSet>
                </reportSets>
            </plugin>
        </plugins>
    </reporting>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>${maven-assembly-plugin.version}</version>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <source>${maven.compiler.source}</source>
                    <target>${maven.compiler.target}</target>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>${maven-exec-plugin.version}</version>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>versions-maven-plugin</artifactId>
                <version>${maven-versions-plugin.version}</version>
                <configuration>
                    <ruleSet>
                        <ignoreVersions>
                            <!-- Pre-release qualifiers -->
                            <ignoreVersion>
                                <type>regex</type>
                                <version>(?i).*[.-]alpha.*</version>
                            </ignoreVersion>
                            <ignoreVersion>
                                <type>regex</type>
                                <version>(?i).*[.-]beta.*</version>
                            </ignoreVersion>
                            <ignoreVersion>
                                <type>regex</type>
                                <version>(?i).*[.-]rc.*</version>
                            </ignoreVersion>
                            <ignoreVersion>
                                <type>regex</type>
                                <version>(?i).*[.-]m\d+.*</version>
                            </ignoreVersion>
                            <ignoreVersion>
                                <type>regex</type>
                                <version>(?i).*-snapshot.*</version>
                            </ignoreVersion>
                            <!-- Date-based nightlies (e.g. 230521-nf-execution, 2021-05-03T08-53-12-abc) -->
                            <ignoreVersion>
                                <type>regex</type>
                                <version>\d{6,}.*</version>
                            </ignoreVersion>
                            <ignoreVersion>
                                <type>regex</type>
                                <version>\d{4}-\d{2}-\d{2}T.*</version>
                            </ignoreVersion>
                            <!-- Misc vendor qualifiers -->
                            <ignoreVersion>
                                <type>regex</type>
                                <version>.*[.-]MR</version>
                            </ignoreVersion>
                            <ignoreVersion>
                                <type>regex</type>
                                <version>.*-[a-z]{2}$</version>
                            </ignoreVersion>
                        </ignoreVersions>
                    </ruleSet>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>${maven-enforcer-plugin.version}</version>
                <executions>
                    <execution>
                        <id>enforce-maven</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireMavenVersion>
                                    <version>${maven.version}</version>
                                </requireMavenVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>${maven-checkstyle-plugin.version}</version>
                <dependencies>
                    <dependency>
                        <groupId>com.puppycrawl.tools</groupId>
                        <artifactId>checkstyle</artifactId>
                        <version>${checkstyle.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <configLocation>${maven.multiModuleProjectDirectory}/config/checkstyle/checkstyle.xml
                    </configLocation>
                    <consoleOutput>true</consoleOutput>
                    <failsOnError>true</failsOnError>
                    <linkXRef>false</linkXRef>
                </configuration>
                <executions>
                    <execution>
                        <id>validate</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>


            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>license-maven-plugin</artifactId>
                <version>${maven-license-plugin.version}</version>
                <configuration>
                    <licenseName>apache_v2</licenseName>
                    <organizationName>${project.organization.name}</organizationName>
                    <inceptionYear>${project.inceptionYear}</inceptionYear>
                    <roots>
                        <root>src</root>
                    </roots>
                    <includes>
                        <include>**/*.java</include>
                        <include>**/*.properties</include>
                        <include>**/*.xml</include>
                    </includes>
                    <excludes>
                        <exclude>**/*Test.java</exclude>
                        <exclude>**/*Tests.java</exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <id>update-file-header-license</id>
                        <goals>
                            <goal>update-file-header</goal>
                        </goals>
                        <phase>process-sources</phase>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-gpg-plugin</artifactId>
                <version>${maven-gpg-plugin.version}</version>
                <executions>
                    <execution>
                        <id>sign-artifacts</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>sign</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
                <version>${maven-central-publishing-plugin.version}</version>
                <extensions>true</extensions>
                <configuration>
                    <publishingServerId>central</publishingServerId>
                    <autoPublish>true</autoPublish>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
